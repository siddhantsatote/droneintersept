{
  "name": "drone-detector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview"
  },
  "dependencies": {
    "@roboflow/inference-sdk": "^0.3.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
